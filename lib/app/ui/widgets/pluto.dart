import 'package:flutter/material.dart';

class Pluto extends StatelessWidget {
  const Pluto({super.key});

  @override
  Widget build(BuildContext context) {
    debugPrint('Pluto build');
    return const Text('Pluto');
  }
}
